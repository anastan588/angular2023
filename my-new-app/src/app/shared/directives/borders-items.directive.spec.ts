import { BordersItemsDirective } from './borders-items.directive';

describe('BordersItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new BordersItemsDirective();
    expect(directive).toBeTruthy();
  });
});
